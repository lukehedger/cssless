import React from "react";
import ReactDOM from "react-dom";

const App = () => (
  <div className="text-lg text-blue-600">Welcome, to the CSSless future ✨</div>
);

ReactDOM.render(<App />, document.getElementById("react-root"));
